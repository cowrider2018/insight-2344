# research/ — 實驗用程式碼（非 production）

本目錄的腳本只供 `/self-explore` 實驗循環使用，**禁止被 daily path**（`build_dataset.py`／`scoring.py`／每日技能）import。
每支腳本對應一筆 `docs/experiments/EXP-NNN-*.md` 紀錄；索引見根目錄 `EXPERIMENTS.md`。
